<%= form_for(reunion_participante) do |f| %>
  <% if reunion_participante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reunion_participante.errors.count, "error") %> prohibited this reunion_participante from being saved:</h2>

      <ul>
      <% reunion_participante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reunion_id %>
    <%= f.number_field :reunion_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
