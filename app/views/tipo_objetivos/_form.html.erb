<%= form_for(tipo_objetivo) do |f| %>
  <% if tipo_objetivo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_objetivo.errors.count, "error") %> prohibited this tipo_objetivo from being saved:</h2>
      <ul>
      <% tipo_objetivo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>  
<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title"  align="center" >Nuevo tipo de Objetivo Mensual</h3>
  </div>
  <div class="panel-body">
   <div class="form-group">
  <label class="col-sm-2 control-label">Descripción del tipo de objetivo</label>  
    <div class="col-sm-10">
      <%= f.text_field :descripcion %>
      </div>
  </div>
  </br>  
<div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" ,value: "Guardar"%>
    </div>
  </div>
    <%= link_to tipo_objetivos_path, class: 'btn btn-primary' do %>
    <%= fa_icon "arrow-left", text: "Atras" %>
  <% end %>
      <%= link_to 'Mostrar', @tipo_objetivo , class: "btn btn-primary" ,value: "Mostrar" %>
  </div>
<% end %>
</div>
</div>
