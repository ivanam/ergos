<%= form_for(carga_diarium, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if carga_diarium.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h5><b><%= fa_icon "exclamation-circle", text: "Atención! Ocurrieron los siguientes errores" %></b></h5>
      <ul>
      <% carga_diarium.errors.messages.values.each do |msg| %>
        <%msg.each do  |m| %>
          <li><%= m %></li>
        <%end %>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :fecha, class: "col-sm-2 control-label" %>
    <div class="col-sm-2">
      <%= f.text_field :fecha, class: "form-control", readonly: true %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :cantidad, "Cantidad", class: "col-sm-2 control-label" %>
    <div class="col-sm-2">
      <%= f.text_field :cantidad, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :tipo_objetivo_id, "Tipo de objetivo", class: "col-sm-2 control-label" %>
    
    <%= f.label :tipo_objetivo_id, carga_diarium.tipo_objetivo, class: "col-sm-2 control-label" %>
    
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" ,value: "Guardar"%>
    </div>
  </div>
<% end %>
