<%= form_for(reunion) do |f| %>
  <% if reunion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reunion.errors.count, "error") %> prohibited this reunion from being saved:</h2>

      <ul>
      <% reunion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fecha %>
    <%= f.date_select :fecha %>
  </div>

  <div class="field">
    <%= f.label :semana %>
    <%= f.number_field :semana %>
  </div>

  <div class="field">
    <%= f.label :lugar_fisico %>
    <%= f.text_field :lugar_fisico %>
  </div>

  <div class="field">
    <%= f.label :persona_id %>
    <%= f.number_field :persona_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
